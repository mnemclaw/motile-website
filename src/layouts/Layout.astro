---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Motile is a self-hosted, open-source link-in-bio builder with six design-forward themes, purposeful Framer Motion + GSAP animations, and a visual editor built right into your page.",
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <!-- TODO: Add Open Graph / Twitter Card meta tags -->
    <!-- TODO: Add canonical URL -->
    <!-- TODO: Add Google Fonts or custom font -->
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="antialiased">
    <slot />
  </body>
</html>
